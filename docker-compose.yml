version: '3.8'

services:
  mediatracker:
    build: .
    container_name: media-tracker
    ports:
      - "8080:8080"
    volumes:
      - ./docker_data:/app/db
    environment:
      - SPRING_DATASOURCE_URL=jdbc:sqlite:/app/db/media.db
    restart: unless-stopped